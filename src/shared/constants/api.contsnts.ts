
export enum TypesFileEnum {
    audio= "audio",
    imageMessage = "image message",
    avatar = "avatar"
}

export enum MainEndpoints {
    users = "/users",
    friend = "/friend",
    dialog = "/dialog",
    blog = "/blog",
    reaction = "/reaction"
}